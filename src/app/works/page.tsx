"use client";
import WorkPage from "@/feature/components/WorkPage";
import ClientLayout from "@/client/ClientLayout";

const Home = () => {
  return (
    <ClientLayout>
      <WorkPage />
    </ClientLayout>
  );
};

export default Home;
